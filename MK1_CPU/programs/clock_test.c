/* Minimal clock-rate test — just spins forever. RUNHZ runs a fixed
 * cycle count then reports khz, which tells us the actual rate
 * achieved for a given ns_per_half setting. */
void main(void) {
    while (1);
}
